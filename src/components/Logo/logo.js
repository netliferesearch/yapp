import React from "react";
import { View, Image } from "react-native";
import * as yappLogo from '../../assets/logo';

export default class Logo extends React.Component {
    render() {
        <View>
            <Image source={yappLogo}></Image>
        </View>
    }
}
